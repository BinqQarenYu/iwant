---
description: Question Only; For Discussion; No Coding: Just Deep research in our App
---

// turbo-all
1. Research Mode Initialization
   - Declare that this session is for **Questioning and Research Only**.
   - **Constraint**: No source code changes are allowed.
   - **Objective**: Map the "Microstructure" and "Forensic" definitions for the feature in question.

2. Context Retrieval (Look Backward)
   - Read the relevant `.md` files in `/docs`.
   - Use `grep` or `find` to map the data flow for the "Analysis Subject."
   - Identify if the feature maps to `algo_trades.duckdb` (Simulation Integrity).

3. Deep Discussion (Socratic Method)
   - Focus on defining the "Truth Filter" or "Microstructure" fingerprints.
   - Reference specific lines in `src/ai/flows` or `src/components/order-flow` for all discussion points.
   - Ask the USER: "What is the primary manipulation signature we are trying to isolate?"

4. Implementation Tax Assessment (Rethink)
   - Estimate the RAM/CPU impact of any *hypothetical* solutions.
   - Discuss how the "Cleanup Rule" or "Performance Guards" would apply if this were to be implemented.

5. Final Synthesis
   - Summarize the research findings.
   - Highlight the "Scale Validated" state of the current architecture.
   - Propose a conceptual roadmap that aligns with the Gold Standard Protocol.
